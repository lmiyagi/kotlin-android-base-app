<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View" />

        <variable
            name="data"
            type="br.com.leonardomiyagi.kotlinbaseapplication.presentation.core.databinding.PlaceholderData" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/loadingBg"
        android:clickable="true"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="16dp"
        android:visibility="@{data.show ? View.VISIBLE : View.GONE, default = gone}">

        <ImageView
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_margin="16dp"
            android:src="@{data.iconDrawable}"
            android:visibility="@{data.iconDrawable == null ? View.GONE : View.VISIBLE}" />

        <ProgressBar
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_margin="16dp"
            android:visibility="@{data.loading ? View.VISIBLE : View.GONE}" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:text="@{data.message}"
            android:textAlignment="center"
            android:textSize="16sp"
            android:visibility="@{data.message == null || data.message.trim().empty ? View.GONE : View.VISIBLE}"
            tools:text="@string/error_unexpected" />

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="16dp"
            android:onClick="@{() -> data.executeTryAgain()}"
            android:text="@string/global_try_again"
            android:visibility="@{data.tryAgain ? View.VISIBLE : View.GONE}" />
    </LinearLayout>
</layout>